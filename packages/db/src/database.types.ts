// PLACEHOLDER. Real types are generated from the live Supabase schema, not
// hand-written — see docs/adr/0012-raw-sql-migrations.md.
//
// Once a Supabase project exists and the migration in
// supabase/migrations/20260101000000_initial_schema.sql has been applied to
// it, regenerate this file with:
//
//   SUPABASE_PROJECT_ID=<project-ref> pnpm --filter @aulawm/db generate-types
//
// Do not hand-edit the generated output — re-run the command after every
// migration instead, so this file never drifts from the real schema.
export type Database = Record<string, never>;
